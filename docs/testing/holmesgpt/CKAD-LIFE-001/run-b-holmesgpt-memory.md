# CKAD-LIFE-001 — Run B: HolmesGPT + POM Memory (Mem0 OSS)

**Trạng thái:** Chưa thực hiện

File này sẽ được cập nhật sau khi chạy cùng bài test với:

- cùng model LiteLLM và tham số điều khiển;
- cùng prompt và oracle độc lập;
- cùng fault nhưng tên resource/run ID mới;
- PostgreSQL chỉ chứa resolution đã được kỹ sư xác nhận và Mem0 chỉ lập chỉ mục các bản ghi đã duyệt;
- ghi lại resolution ID, memory ID, độ liên quan, fingerprint differences và provenance;
- kiểm tra cả paraphrase và hard negative cùng triệu chứng nhưng khác nguyên nhân;
- ghi nhận số lượt kỹ sư phải sửa hoặc bổ sung trước khi xác nhận resolution.

Không điền kết quả trước khi Run B thực sự hoàn tất và được chấm độc lập.
